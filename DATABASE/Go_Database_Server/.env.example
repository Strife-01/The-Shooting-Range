# Environment variables for the Go backend

DB_HOST=db
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=shooting_game

SERVER_PORT=8080

# Connection string for the Go server to reach Postgres inside Docker
DB_URL=postgres://postgres:postgres@db:5432/shooting_game?sslmode=disable
