
// x = lerp(x, device_mouse_x(0), 0.98);
// y = lerp(y, device_mouse_y(0), 0.98);

x = device_mouse_x(0);
y = device_mouse_y(0);

if (room = rm_game) {
	//visible = false;
} else { 
	visible = true;
	}